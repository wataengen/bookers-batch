<!DOCTYPE html>
<html>
  <head>
   <title>WEBCAMP 基礎学習フェーズ　課題2</title>
  </head>
  <body>


<h1><string>Books</string></h1>
<table>
  <thead>
    <th>Title</th>
    <th>Body</th>
    <th colspan="3"></th>
  </thead>
  <tbody>
    <% @books.each do |book| %>
    <tr>
     <th><%= book.title %></th>
     <th><%= book.body %></th>

     <th><%= link_to "Show", book_path(book.id) %></th>
     <th><%= link_to "Edit", edit_book_path(book.id) %> </th>
     <th><%= link_to "Destroy", destroy_book_path(book.id), method: :delete, "data-confirm" => "Are you sure?" %>
     </th>
    </tr>
    <% end %>

  </tbody>
</table>
<h2><string>New Book</string></h2>

<div id="errors">
<% if @book.errors.any? %>
     <h4><%= @book.errors.count %> error prohibited this book from being saved:</h4>
    <ul>
      <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
    </ul>
<% end %>
</div>

<%= form_with model:@book,url: '/books',method: :post do |f| %>
  <h4>Title</h4>
  <ul><%= f.text_field :title,{size:"30"} %></ul>

  <h4>Body</h4>
  <ul><%= f.text_area :body,{size:"32x5"} %></ul>

  <ul><%= f.submit 'Create Book' %></ul>
<% end %>


  </body>
</html>