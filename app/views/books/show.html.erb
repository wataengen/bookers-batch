<!DOCTYPE html>
<html>
  <head>
   <title>WEBCAMP 基礎学習フェーズ　課題2</title>
  </head>
  <body>
    <div>
      <%= flash[:notice] %>
    </div>

    <p><strong>Title:</strong><%= @book.title %></p>
    <p><strong>Body:</strong><%= @book.body %></p>

    <%= link_to "Edit", edit_book_path(@book.id) %>
    <span>|</span>
    <%= link_to "Back", "/books" %>
  </body>
</html>